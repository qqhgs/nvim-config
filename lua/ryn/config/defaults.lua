local defaults = {
  leader = "space",
  format_on_save = false,
  timeoutlen = 200,

  keys = {},

  plugins = {
    -- use config.lua for this not put here
  },

  builtins = {},
}

return defaults
